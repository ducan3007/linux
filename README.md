## Các lệnh cơ bản
+ `cd` : Di chuyển đển thư mục khác

+ `ls` : Liệt kê nội dung thư mục
   + `ls -l` : Gồm 7 trường:  `- rw- rw- r--.` `1` `owner` `lilo` `1024`  `Feb 26 07:08` `file1`
   
### `hard-link`  `soft-link`:

## Quyền truy cập `- rw- rw- r--`

+ `-` : file
+ `d` : thư mục

+ `rw-`: 1 là quyền User
+ `rw-`: 2 là quyền
+ `rw-`: 3 là quyền

### Chỉ số inode: `ls -i` là chỉ số 
